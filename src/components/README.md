# UI Components

This folder contains UI building blocks and page sections for the DripCode Studio site.

- `ui/` holds shadcn-style primitives (e.g. Button).
- Section components live at the root and are composed in `page.tsx`.
